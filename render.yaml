services:
  - type: web
    name: embroidery-desktop-backend
    env: node
    plan: free
    buildCommand: cd desktop-backend && npm install
    startCommand: cd desktop-backend && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
